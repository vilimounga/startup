I learned the basics of writing markdown and how to push and pull with git.
## History of the Web
The Web Father (HTTP, HTML, URL) - Tim Berners-Lee. They were trying to find a better method to deliver documents and make it accessible to everyone. (DNS HTTP)
Hakon Wium Lie - CSS. He wanted to make a way to format designs from the HTML structures.
Brendan Eich - JavaScript
## Amazon Web Services (EC2)
I learned tonight how to rent a server through AWS and set it up. I had trouble tonight with using the shell remote function but I will update this once I get help from TAs/Classmates.
The Public IP address I created was 44.206.230.48. The Elastic IP Address I associated with my Instance was 34.195.93.86
## CSS Development
CSS is the actual design and appearance of our HTML structures. We use rules that consist of selecting HTML elements and giving it specific types and values to apply.
CSS Frameworks are pretty much like sets of codes that we can apply in our own HTML code that can make it easier and faster to apply built-out CSS styles.
2 Examples of CSS Frameworks are Bootstrap and Tailwind CSS.
Bootstrap applys the style rules within the class of the HTML elements and not in the CSS stylesheets like vanilla CSS.
Bootstrap is built on Flexbox which makes it easy for the programmer to have responsive containers on their page with not much code.
Some of the example style rules used in the classes from Bootstrap are: "d-md-flex, container, p-5, mx-2, pb-3, bg-primary, text-warning etc"
Used the debugger in the web browser to easily see the physical aspects of the CSS rules and selectors. 
I also learned that selecting specific elements, you can also see what that element inheirts different styles from parent elements. 
Javascript script from Bootstrap is needed in order to get some functionality for the different elements like the nav-bar, icons, and input forms.
Flexbox/Grid are important to the responsive aspect of the webpage because it helps to resize the boxes on the page when making the page smaller. 